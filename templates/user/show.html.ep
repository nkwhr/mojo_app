% layout 'default';

<p>
  <strong>Name:</strong>
  <%= $user->{name} %>
</p>

<p>
  <strong>Email:</strong>
  <%= $user->{email} %>
</p>

<a href="<%= url_for('edit_user_path', id => $user->{id}) %>" class="small">Edit</a> |
<a href="<%= url_for('user_path') %>" class="small">Back</a>
